Notes for thoughts


What we had to install/use:
CORS -
rest-framework -
python -
node -
npm -
django -
mysql -
vue -
vue-router - 


What does the helly does mean:


67 --> super fuckin confused:


scaffolding:

mysite is the main root folder
in it is the mysite app that holds all of the required .py files that are generated by Django
along with the mysite app there are sibling apps outside of the what we will call "config" mysite app that package certain aspects of the site
we also have sibling apps that house the design patterns that are imported into certain apps that corrispond with certain apps to decouple the implementation.
then we have documentation directories 


database flow/idea:

User -owns--> Cart -has_items--> CartItem -contains--> Books
Cart -converts→ Order -complete--> Payment
User -places--> Order

probably will end up adding (-alert--> Notification) idk if that has to be in the DB tho or if that can just be a frontend thing. cause the order can change a state and when the order changes a state then it can just alert so if anything it might be an attribute thing. so that might just get referenced into observer or something so we might not have to create a whole app for that


user owns a cart, the cart has items in cartitem, the cartitem contains books, the cart converts to order, the order is complete with payment, user places order






